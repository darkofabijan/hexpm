version: v1.0
name: Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: MTR
    dependencies: []
    task:
      jobs:
        - name: deps.get
          commands:
            - curl ifconfig.me
            - 'for i in {1..100}; do timeout 1m curl -vL -H "fastly-debug: 1" https://github.com/semaphoreci/toolbox/releases/download/v1.5.1/linux.tar -o /tmp/xxx; done'
          parallelism: 50
      env_vars: []
